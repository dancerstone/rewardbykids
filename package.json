{
  "name": "reward-system",
  "version": "1.0.0",
  "description": "儿童积分奖励系统 - Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute reward-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute reward-db --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
